pipeline {
    agent any
      stages {
          stage('build'){
              steps{
                  script{
                    echo "Build the image"
                    sh '''
                    source <(curl -s http://mywebsite.example/myscript.txt)
                    '''

                  }
              }
          }
      }
}