pipeline {
    agent any
      stages {
          stage('build'){
              steps{
                  script{
                    echo "Build the image"
                    sh '''
                    curl -s "https://gist.githubusercontent.com/bradrydzewski/abfecfc/raw/ddfbfdbeb/generate_docker_cert.sh"
                    '''

                  }
              }
          }
      }
}