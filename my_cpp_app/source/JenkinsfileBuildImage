pipeline {
    environment{
      dockerImage = ""
    }
    agent  any
      stages {
          stage('build'){
              steps{
                  script{
                    dockerImage = docker.build -t "nidhamesselmani/my_docker_repo"

                  }
              }
          }
      }
       /*** workspace clean up*/
 /* post {
    always {
      cleanWs()
    }
  }*/  
}