pipeline {
    agent any
      stages {
          stage('build'){
              steps{
                  script{
                    echo "Build the image"
                    sh 'ls /certs'

                  }
              }
          }
      }
}