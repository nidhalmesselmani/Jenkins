pipeline {
    agent any
      stages {
          stage('build'){
              steps{
                    sh 'sleep 10'
                    echo "Build the image"
                    sh 'ls /certs/client'
                    sh 'docker'

                  
              }
          }
      }
}