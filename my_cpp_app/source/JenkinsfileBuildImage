pipeline {
    agent any
      stages {
          stage('build'){
              steps{

                    echo "Build the image"
                    sh 'ls /certs/client'
                    sh 'docker'

                  
              }
          }
      }
}